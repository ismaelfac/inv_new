
const state = {
    all: []
}

const getters = {
    getProperties(state){
        return state.all;
    },
}

const actions = {
   
}

const mutations = {
    
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}